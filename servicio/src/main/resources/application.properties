spring.application.name=servicio-jpa

# Puerto de la aplicacion
server.port=8082

# === CONEXION PostgreSQL (misma BD que Django - Neon) ===
# La URL de Neon incluye ?sslmode=require para conexiones seguras
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-crimson-paper-ackquvpw.sa-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_HqiK5hdUcB4Y}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.maximum-pool-size=${SPRING_DATASOURCE_POOL_SIZE:10}

# === JPA / Hibernate ===
# validate: verifica que la entidad coincida con la tabla existente (no modifica la BD)
# IMPORTANTE: no usar 'update' ni 'create' ya que la tabla la gestiona Django
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# === Seguridad ===
spring.security.user.name=admin
spring.security.user.password=admin123
spring.autoconfigure.exclude=org.springframework.boot.security.autoconfigure.UserDetailsServiceAutoConfiguration

# === CORS ===
app.cors.allowed-origins=${APP_CORS_ORIGINS:http://localhost:3000,http://localhost:5173,http://localhost:8080}

# === Rate Limiting (proteccion contra peticiones excesivas) ===
app.rate-limit.get-per-minute=120
app.rate-limit.post-per-minute=30
app.rate-limit.put-per-minute=30
app.rate-limit.delete-per-minute=20

# === Paginacion ===
app.producto.default-page-size=10
app.producto.max-page-size=100
app.producto.max-page-number=10000

# === Seguridad del servidor ===
server.tomcat.max-http-form-post-size=128KB
server.tomcat.max-swallow-size=128KB
server.tomcat.max-parameter-count=100
spring.web.error.include-stacktrace=never
spring.web.error.include-message=never
spring.web.error.include-exception=false
spring.web.error.include-binding-errors=never
spring.web.error.include-path=never

# === Logging ===
logging.file.name=logs/servicio.log
logging.logback.rollingpolicy.file-name-pattern=logs/servicio-%d{yyyy-MM-dd}.%i.log.gz
logging.logback.rollingpolicy.max-file-size=10MB
logging.logback.rollingpolicy.max-history=7
logging.logback.rollingpolicy.total-size-cap=100MB
logging.level.com.example.servicio=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=WARN
